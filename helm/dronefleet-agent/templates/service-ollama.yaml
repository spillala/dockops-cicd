apiVersion: v1
kind: Service
metadata:
  name: dronefleet-agent-ollama-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: dronefleet-agent-ollama
    part-of: dockops
spec:
  selector:
    app: dronefleet-agent-ollama
  ports:
    - name: http
      port: 11434
      targetPort: 11434
      protocol: TCP
  type: ClusterIP
